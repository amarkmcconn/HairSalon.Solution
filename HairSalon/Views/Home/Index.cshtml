@{
  Layout = "_Layout";
}

<div class="container">
  <h2>Eau Claire's Salon</h2>
  <img src='~/img/salon.jpg'>
  <h3>Stylist and Client Management</h3>
  @using (Html.BeginForm("Index", "Clients", FormMethod.Get))
  {
    <input type="submit" value="See All Clients" class="btn btn-primary">
  }
  <br>@using (Html.BeginForm("Index", "Stylists", FormMethod.Get))
  {
    <input type="submit" value="See All Stylists" class="btn btn-secondary">
  }
</div>
